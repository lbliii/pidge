{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "pidge serve --host 0.0.0.0 --port $PORT --no-debug",
    "healthcheckPath": "/ready",
    "healthcheckTimeout": 300,
    "overlapSeconds": 10,
    "drainingSeconds": 20,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
